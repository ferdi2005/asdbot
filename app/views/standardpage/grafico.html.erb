<% unless @sender.nil? %>
  <%= line_chart Asd.where(sender: @sender).group_by_day(:created_at).totalcount %>
<% end %> 

<% unless @group.nil? %>
    <%= line_chart Asd.where(group: @group).group_by_day(:created_at).totalcount %>
<% end %>
